version: 2

before:
  hooks:
    - go mod tidy

builds:
  - id: polywave
    main: ./cmd/polywave
    binary: polywave
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X main.version={{.Version}}

archives:
  - id: polywave
    format: tar.gz
    format_overrides:
      - goos: windows
        formats: [zip]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    files:
      - LICENSE*
      - README*
      - CHANGELOG*

checksum:
  name_template: 'checksums.txt'

snapshot:
  name_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^chore:'
      - Merge pull request
      - Merge branch
  groups:
    - title: Features
      regexp: '^feat:'
      order: 0
    - title: Bug Fixes
      regexp: '^fix:'
      order: 1
    - title: Others
      order: 999

release:
  github:
    owner: blackwell-systems
    name: polywave-go
  draft: false
  prerelease: auto

# Brew publishing disabled - requires PAT with cross-repo permissions
# Formula is manually maintained in blackwell-systems/homebrew-tap
# brews:
#   - name: polywave
#     repository:
#       owner: blackwell-systems
#       name: homebrew-tap
#       token: "{{ .Env.GITHUB_TOKEN }}"
#     directory: Formula
#     homepage: https://github.com/blackwell-systems/polywave-go
#     description: "CLI for the Polywave parallel agent coordination protocol"
#     license: MIT
#     install: |
#       bin.install "polywave"
#     test: |
#       system "#{bin}/polywave", "--version"
